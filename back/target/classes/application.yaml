#配置springboot
spring:
  datasource:
    #jdbc连接基础配置
    #账户名
    username: root
    password: 'yuri2233'
    #本地的数据库url
    #后面是demo 数据库名字
    url: jdbc:mysql://127.0.0.1:3306/demo?characterEncoding=UTF-8&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    #高版本mysql驱动，driver-class-name后面需要设置时区
